# Copilot Instructions

## Project Guidelines
- User wants any client to be able to view provider base location (no ownership check on `GetBaseLocation`).
- User wants to avoid surfacing "username already exists" errors to end users; prefer email-specific/consistent errors during registration.